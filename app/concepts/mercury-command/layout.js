import "./mercury-command.css";

export const metadata = {
  title: "NEXT Command — AI Financial Operator",
  description: "Mercury Command의 파티클 Canvas와 스크롤 오케스트레이션을 기반으로 한 NEXT 증권 콘셉트."
};

export default function MercuryCommandLayout({ children }) {
  return children;
}
