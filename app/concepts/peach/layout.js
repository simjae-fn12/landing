import "./peach.css";

export const metadata = {
  title: "NEXT 증권 — Compliance-by-Design",
  description: "Media-First Trading, AI-Native Intelligence와 Headless Architecture를 담은 NEXT 증권 콘셉트."
};

export default function PeachConceptLayout({ children }) {
  return children;
}
